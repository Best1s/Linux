网卡的7个模块
CU:	Control Unit
OB:	Output Buffer
IB:	Input Buffer
LC:	Line Coder
LD:	Line Decoder
TX:	Transmitter
RX:	Receiver

以太帧两种  IEEE8.2.3  和 internet 2  也成为 DIX格式（常用）  网卡都兼容
单播帧
组播帧
广播帧

ARP

STP
1:MAC地址表翻摆     2：广播风暴		3：多帧复制
BID(Bridge Identifier) PID(Port Identifier)
运行STP协议的交换机会交换STP帧，帧的载荷数据为BPDU(Bridge Protocol Data Unit) 非网络层数据单元产生者为交换机
选举最小的BID为桥根
根据路径开销最小的（Root Path Cost,RPC）选定桥端口
   端口速率			路径开销(IEEE 802.1t标准)
	10Mbit/s		   2 000 000
	100Mbit/s			 200 000
	1Gbit/s				  20 000
	10Gbit/s			   2 000
如果非根桥RPC可能相同，比较上行设备的BID，BID不同所连上行设备BID较小的端口为根端口，BID相同比较上行设备的PID，较小的端口为根端口
https://www.binver.top/images/STP_root_port.png
确定根端口后剩余的非根端口和非指定端口统称为备用端口，STP会进行逻辑阻塞
STP协议是一种组播帧 组播地址为 01-08-c2-00-00-00

RSTP  MSTP(常用)

VLAN
IEEE 802.1Q帧
GARP  （GMRP    GVRP）  动态vlan

ip报文格式  包
单播包
组播包
广播包

tcp报文格式  段

IGP协议中
RIP
直连路由
静态路由
动态路由
RIP-v2兼容v1
RIP协议使用了三种定时器，分别是更新定时器（周期值30s），无效定时器(180s)，垃圾收集定时器(120s)。
RIP解决环路的三种方法，  触发更新	水平分割	毒性逆转

OSPF报文
Hello报文，
数据库描述报文(Database Description Packet,DD Packet)
链路请求报文（Link-State Request Packet,LSR Packet）
链路状态更新报文（Link-State Update Packet,LSU Packet）
链接状态确定报文（Link-State Acknowledgement Packet, LSAck Packet）
单区域，多区域
骨干区域，非骨干区域，  骨干路由（BR），区域边界路由器(ABR)，自治系统边界路由器(ASBR)


IS-IS

EGP中
BGP

LACP 链路聚合控制协议(Link Aggregation Control Protocol)
手工负载（默认）   自动（会自己协商）


Smart Link  华为私有协议 替代STP 实现毫秒级链路切换
Monitro Link 配合Smart Line 使用 华为私有协议   上行口 于下行口 端口状态保持一致

DHCP
续租规则：  T1 时刻租约到一半  T2时刻租约到 87.5%   默认1天

NAT    NAPT  Easy IP  动态  静态

PPP      PPPOE

ACL  

snmp  端口号 get UDP 161 , snmp trap udp 162


















-------------------------------------===========------------------------------------
ISO/OSI  TCP/IP协议族  
以太网帧，IP数据报，TCP段 UDP段 格式
PPP协议
IPV6
MAIL SMTP POP DNS 协议

RIP工作原理以及自身局限
RIP路由循环
RIP定时器
RIPV1 RIPV2对比

OSPF工作原理  Router ID
OSPF中COST值计算
OSPF链路及链路传递方式
OSPF区域
OSPF邻居及邻接
OSPF中DR,BDR

ACL访问控制列表基本原理
ACL正确放置位置
ACL定义规范及命名格式

NAT技术原理及功能
NAT实现方式
NAT技术背景及传递原理

CIDR
VLSM

VLAN

IDS
IPS

VRRP
HSRP

STP
BPDU

SAN NAS DAS
FC-SAN IP-SAN NAS DAS 对比分析

















